class TodoItemClass {
    id: number;
    description: string;
    constructor(id:number, message: string) {
        this.description = message;
        this.id = id;
    }
}

export default TodoItemClass;